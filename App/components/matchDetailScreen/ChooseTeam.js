import React from 'react';
import {View, Text, StyleSheet, Button} from 'react-native';

const ChooseTeam = ({navigation, route}) => {
  return (
    <View style={{flex: 1}}>
      <Text>Choose League</Text>
      <Button title="Nowhere to Go" />
    </View>
  );
};

export default ChooseTeam;
